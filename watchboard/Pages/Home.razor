@using Microsoft.AspNetCore.Components.Sections
@using WatchBoard.Pages.Shared
@using WatchBoard.Pages.Partials
@using WatchBoard.Database.Entities
@layout _Layout

<SectionContent SectionName="nav-title">
    <button type="button" class="navbar-brand nav-link dropdown-toggle" data-bs-toggle="dropdown"
            aria-expanded="false">
        <i class="bi bi-person-workspace"></i> @SelectedBoard.Name
    </button>
    <ul class="dropdown-menu">
        @foreach (var b in Boards.OrderBy(x => x.Order))
        {
            <li>
                @if (b.Id == SelectedBoard.Id)
                {
                    <a class="dropdown-item disabled" href="/?boardId=@(b.Id)">@b.Name</a>
                }
                else
                {
                    <a class="dropdown-item" href="/?boardId=@(b.Id)">@b.Name</a>
                }
            </li>
        }
    </ul>
</SectionContent>

<SectionContent SectionName="nav-end">
    <li class="nav-item">
        <button class="nav-link me-3"
                hx-get="/app/search"
                hx-target="#search-modals-here" hx-trigger="click" hx-swap="innerHTML"
                data-bs-toggle="modal" data-bs-target="#search-modals-here"
                aria-controls="searchDrawer">
            <i class="bi bi-search"></i> Search
        </button>
    </li>
</SectionContent>

<div class="container-fluid px-md-4 pt-2">
    <div>
        @foreach (var l in Lists)
        {
            <div hx-get="/app/lists/@(l.Id)" hx-trigger="newItem from:body" hx-target="this">
                <_List ListModel="l"></_List>
            </div>
        }
    </div>

    <!-- Bootstrap Modal for ItemDetails -->
    <div id="modals-here" class="modal modal-blur fade" style="display: none" aria-hidden="false" tabindex="-1">
        <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
            <div class="modal-content">XXX</div>
        </div>
    </div>

    <!-- Bootstrap Modal for Search -->
    <div id="search-modals-here" class="modal modal-blur fade" style="display: none" aria-hidden="false" tabindex="-1">
        <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
            <div class="modal-content">XXX</div>
        </div>
    </div>

</div>

@code {

    [Parameter]
    public List<List> Lists { get; set; } = [];

    [Parameter]
    public List<Board> Boards { get; set; } = [];

    private Board SelectedBoard => Boards.FirstOrDefault(x => x.Id == Lists[0].BoardId) ?? Boards.First();
}